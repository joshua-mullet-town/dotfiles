# macOS
.DS_Store

# iTerm2 auto-generated files (keep the main config)
iterm2/*.bak

# Claude MCP secrets (actual API keys)
claude/mcp-secrets.json

# Machine-specific config (code directory path)
claude/machine-config.json
